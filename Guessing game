import random
n = random.randint(1, 20)
print("১-২০ এর মধ্যে সংখ্যা ধরো!")
while True:
    g = int(input("👉 অনুমান: "))
    if g == n:
        print("🎯 ঠিক ধরেছো!")
        break
    print("⬆️ বড়" if g < n else "⬇️ ছোট")
