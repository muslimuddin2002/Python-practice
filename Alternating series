# Alternating series
def alt_series(n):
    total = 0
    for i in range(1, n+1):
        if i % 2 == 0:
            total -= i
        else:
            total += i
    return total

n = 10
print("Alternating Sum:", alt_series(n))
